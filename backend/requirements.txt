openai>=2.8
google-genai>=1.33
# Upper-bounded: the deployed image resolved FastAPI 0.141 / Starlette 1.6
# while this machine had 0.122 / 0.50, and the startup hook that worked locally
# silently never ran in production. Pin the majors so local and deployed agree.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8
python-dotenv>=1.0
httpx>=0.27
beautifulsoup4>=4.12
python-multipart>=0.0.9
