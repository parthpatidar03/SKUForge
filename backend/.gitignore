.env
__pycache__/
*.pyc
cache/
# The record store and every backup prune writes beside it — local runtime
# data, binary, and it churns on every run. `skuforge.db` alone missed the
# .bak/.bak2 files prune creates.
*.db
*.db.bak*
.venv/
